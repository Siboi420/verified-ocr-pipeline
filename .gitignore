# bytecode / caches
__pycache__/
*.pyc
.pytest_cache/

# runtime data (scanned documents, review state, uploads)
validation/pending/
validation/verified/
validation/rejected/
validation/uploads/

# env / secrets (no hardcoded keys)
*.env
.env*

# chat sessions (one JSON file per session)
sessions/

# logs
*.log